/* Configuration file

   This file define constants that can set up the behavior of code.
 */

#pragma once

/* Maximum queue name size. */
#define MAX_QUEUE_NAME 256

/* Maximum size of the input and the output buffers for each connection. */
#define MAXLINE 131073

/* Maximum number of simultaneous connections. */
#define MAX_CONNECTIONS 512

/* If defined, this enables logging to stdout. */
#define SHOULD_LOG
